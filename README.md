# The following binaries were used instead of the ARM ones from the current varsion of the guide, all of them are listed in `downloads.txt`. Some sctipts are also included as text files in `scripts` directory. The rest is left as is.


